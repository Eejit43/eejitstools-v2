{
    "extends": "@tsconfig/recommended/tsconfig.json",
    "compilerOptions": {
        "target": "ESNext",
        "lib": ["DOM", "DOM.Iterable"]
    },
    "include": ["data/*.ts", "scripts/*.ts", "scripts/pages/**/*.ts"]
}
