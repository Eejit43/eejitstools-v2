<div class="warning scroll-message">Scroll →</div>
<table class="info-table">
    <thead>
        <tr>
            <th>State</th>
            <th>Postal Abbreviation</th>
            <th>Standard Abbreviation</th>
        </tr>
    </thead>
    <tbody>
        <% for (const state of additionalData.stateAbbreviations) { %>
        <tr>
            <td><%= state.name %></td>
            <td><%= state.postal %></td>
            <td><%= state.standard %></td>
        </tr>
        <% } %>
    </tbody>
</table>
